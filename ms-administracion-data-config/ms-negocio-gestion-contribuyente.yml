server:
   port: 8091
   
# MySql #
spring:
  jpa: 
    hibernate: 
      ddl-auto: create
    show-sql: true
  datasource: 
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3308/db_ms_contribuyente?createDatabaseIfNotExist=true&useSSL=true
    username: uncledave
    password: Test123
    driver-class-name: com.mysql.cj.jdbc.Driver

# Server Admin Client
  boot:
    admin:
      client:
        url: http://localhost:8890
        instance:
          management-base-url: http://localhost:8091
          
# Server Registry Client
eureka:
   client:
      fetch-registry: true
      serviceUrl:
         defaultZone: http://localhost:8889/eureka
#   instance:
#      instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}   
      
# Endpoints exposition
# Acuator
management:
  endpoints:
    web:
      exposure:
        include: "health, prometheus"
  health:
    readinessstate:
      enabled: true
    livenessstate:
      enabled: true
  endpoint:
    health:
      probes:
        enabled: true 
  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans
  tracing:
    sampling:
      probability: 1.0
      
#logging:
#  level:
#    root: debug
#  pattern:
#    level: %5p [${spring.application.name}, %X{traceId:-}, %X{spanId:-}]
         